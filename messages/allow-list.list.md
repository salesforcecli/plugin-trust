# summary

List the plugins on the unsigned plugin allow-list.

# description

Prints the contents of the `unsignedPluginAllowList.json` file as a table.

# examples

- List all plugins on the allow-list:

  <%= config.bin %> <%= command.id %>

# NoPluginsAllowListed

No plugins are currently on the allow-list.
